# Multi-Hop 요청 - 30초 요약 (엘리베이터 피치)

## 🎤 백엔드 팀에게 이렇게 말하세요

---

### 버전 1: 기술적 설명 (개발자용)

> "지금 리스크 스코어링 API가 **1-hop 거래만** 분석해서, 그래프 패턴 탐지 룰(B-201 Layering Chain, B-202 Cycle)이 **전혀 작동하지 않고 있어**.
>
> Target 주소의 거래뿐만 아니라, **counterparty 주소들의 거래도 수집**해서 보내줄 수 있을까? 최대 3-hop까지.
>
> 그러면 `Target → Mixer1 → Mixer2 → Clean` 같은 경로를 추적할 수 있어서, 리스크 탐지 정확도가 30-50% 정도 올라갈 거야.
>
> API 파라미터에 `max_hops` 추가하고, 각 거래에 `hop_level` 필드 넣어주면 돼. 캐싱 쓰면 3-8초 정도로 최적화 가능해."

---

### 버전 2: 비즈니스 가치 강조 (PM/매니저용)

> "현재 리스크 스코어링 시스템이 **단순 거래만 탐지**하고, **복잡한 세탁 패턴은 못 잡고 있어요**.
>
> 예를 들어 Tornado Cash → 중간 주소 → CEX 같은 3-hop 세탁 경로를 지금은 탐지 못해요.
>
> 백엔드에서 **multi-hop 거래 수집**을 구현하면:
>
> - 리스크 탐지 정확도 **30-50% 향상**
> - False Positive **20-30% 감소**
> - 사용자 만족도 증가
>
> 개발 기간은 약 **2주**, ROI는 매우 높습니다."

---

### 버전 3: 문제 해결 중심 (이슈 보고용)

> "**버그 발견**: B-201 (Layering Chain), B-202 (Cycle) 룰이 작동하지 않고 있습니다.
>
> **원인**: API가 1-hop 거래만 제공하기 때문에, 그래프 구조를 파악할 수 없습니다.
>
> **해결책**: Multi-hop 거래 수집 구현
>
> - Target의 거래 (1-hop)
> - Counterparty들의 거래 (2-hop, 3-hop)
>
> **예상 효과**: 리스크 탐지 정확도 30-50% 향상"

---

## 📊 한 장으로 보는 변경사항

### Before (1-hop)

```
입력: Target 주소
수집: Target의 거래만
결과: [Target ↔ A], [Target ↔ B]
한계: A와 B의 연결 관계 모름
```

### After (Multi-hop)

```
입력: Target 주소 + max_hops
수집: Target + Counterparty들의 거래
결과: [Target → A → B], [A → C → Target]
장점: 전체 경로 추적 가능
```

---

## 🔑 핵심 요청사항 3줄 요약

1. **API 파라미터 추가**: `max_hops` (기본값: 1, 최대: 3)
2. **수집 로직 변경**: Target의 counterparty 주소들의 거래도 수집
3. **응답 필드 추가**: 각 거래에 `hop_level` 추가

---

## 💬 예상 질문 & 답변

### Q: "얼마나 걸려요?"

**A**: 약 2주 (1주 구현 + 1주 최적화)

### Q: "기존 API가 깨지나요?"

**A**: 아니요, 완전 하위 호환. `max_hops` 없으면 기존대로 동작

### Q: "성능은 괜찮아요?"

**A**: 캐싱 쓰면 3-8초로 최적화 가능. `analysis_type="basic"`일 때는 기존 속도 유지

### Q: "왜 꼭 필요한가요?"

**A**: 복잡한 세탁 패턴 탐지 불가능. B-201, B-202 룰이 **전혀 작동 안 함**

### Q: "우선순위가 어떻게 되나요?"

**A**: **High**. 현재 리스크 탐지의 30-50%가 누락되고 있음

---

## 📧 이메일 템플릿

```
제목: [요청] 리스크 스코어링 Multi-Hop 거래 수집 구현

안녕하세요,

리스크 스코어링 시스템의 그래프 패턴 탐지 기능이 작동하지 않는 이슈를 발견했습니다.

**문제**:
- 현재 API가 1-hop 거래만 제공
- B-201 (Layering Chain), B-202 (Cycle) 룰이 전혀 작동하지 않음
- 복잡한 세탁 패턴 탐지 불가능

**해결책**:
- Multi-hop 거래 수집 구현 (최대 3-hop)
- API 파라미터 `max_hops` 추가
- 각 거래에 `hop_level` 필드 추가

**기대 효과**:
- 리스크 탐지 정확도 30-50% 향상
- False Positive 20-30% 감소

**예상 개발 기간**: 2주

상세 문서는 첨부했습니다:
- SIMPLE_COMPARISON_1HOP_VS_MULTIHOP.md
- BACKEND_REQUEST_MULTI_HOP.md

논의 부탁드립니다.

감사합니다.
```

---

## 🎯 미팅에서 보여줄 데모

### 시나리오: Tornado Cash 세탁 경로

**1-hop (현재)**:

```
Target → Tornado Cash (5000 USD)
↓
E-101 발동 (점수: 25)
↓
Risk Level: LOW ❌ (실제로는 HIGH여야 함)
```

**Multi-hop (필요)**:

```
Target → Tornado Cash → Intermediate → CEX (5000 USD)
↓
E-101 발동 (점수: 25)
B-201 발동 (점수: 40) ← 새로 탐지됨!
↓
Risk Level: HIGH ✅
```

**차이**:

- 점수: 25 → 65 (160% 증가)
- 레벨: LOW → HIGH
- 정확도: 크게 향상

---

## 📋 다음 단계

### 1. 문서 공유

- [ ] SIMPLE_COMPARISON_1HOP_VS_MULTIHOP.md
- [ ] BACKEND_REQUEST_MULTI_HOP.md
- [ ] MULTI_HOP_REQUIREMENT.md

### 2. 미팅 일정 잡기

- [ ] 백엔드 팀과 30분 미팅
- [ ] 데모 준비 (위 시나리오)
- [ ] Q&A 세션

### 3. 구현 스케줄

- [ ] Week 1: Phase 1 구현
- [ ] Week 2: Phase 2 최적화
- [ ] Week 3: 테스트 & 배포

---

**작성일**: 2025-11-21  
**우선순위**: High  
**담당자**: Risk Scoring Team
